- meta title: @post.title, description: @post.description, |
    twitter: {  card: "summary", site: "@andreasknoepfle", title: @post.title, |
    description: @post.description, image: @post.image.present? ? image_post_url(@post, style: :thumb) : "", url: post_url(@post)} |
.container.heading
  .spacer
    %h1= @post.title
    - if user_signed_in?
      .btn-group
        = link_to edit_post_path(@post), class: 'btn btn-primary' do
          %i.fa.fa-pencil>
        - if @post.published
          = link_to post_path(@post, post: {published:false}), method: :put, class: 'btn btn-primary' do
            %i.fa.fa-eye>
        - else
          = link_to post_path(@post, post: {published:true}), method: :put, class: 'btn btn-primary' do
            %i.fa.fa-eye-slash>
        = link_to post_path(@post), method: :delete, class: 'btn btn-danger' do
          %i.fa.fa-times>

.container.white

  .row
    .col-md-9
      .spacer
        .pull-right
          - if @post.image.present?
            = link_to image_post_path(@post), class: "img-spacer" do
              = image_tag(image_post_path(@post, style: :bigger), class: "img-thumbnail")
        by Andreas Knöpfle
        = time_ago_in_words @post.updated_at
        ago
        %hr
        .description

          = @post.description
        .post
          = raw @post.content
          .clearfix
        .comments
          .spacer
          #disqus_thread
          :javascript
            /**
            *  RECOMMENDED CONFIGURATION VARIABLES: EDIT AND UNCOMMENT THE SECTION BELOW TO INSERT DYNAMIC VALUES FROM YOUR PLATFORM OR CMS.
            *  LEARN WHY DEFINING THESE VARIABLES IS IMPORTANT: https://disqus.com/admin/universalcode/#configuration-variables*/
            /*
            var disqus_config = function () {
            this.page.url = 'https://www.smallbutton.com';  // Replace PAGE_URL with your page's canonical URL variable
            this.page.identifier = #{}; // Replace PAGE_IDENTIFIER with your page's unique identifier variable
            };
            */
            (function() { // DON'T EDIT BELOW THIS LINE
            var d = document, s = d.createElement('script');
            s.src = '//smallbutton.disqus.com/embed.js';
            s.setAttribute('data-timestamp', +new Date());
            (d.head || d.body).appendChild(s);
            })();

        = link_to "Back", posts_path, class: 'btn btn-default', role: "button"
    .col-md-3
      .spacer
        - @other_posts.each do |post|
          = link_to post do
            %h4= post.title
          %p= post.description

          %hr
        .more.pull-right
          = link_to "more", posts_path
